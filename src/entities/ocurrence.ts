export interface Ocurrence {
	ocurrence_id: string
	client_id: string
	observer_id: string
	creation_date: string
	preview_message: string
	ocurrence_url: string
	is_recovery: boolean
	org_id: string
}

